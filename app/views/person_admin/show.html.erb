  <table>
    <tr>
      <td>First Name</td>
      <td><%=h @person.first_name %></td>
    </tr>
    <tr>
      <td>Last Name</td>
      <td><%=h @person.last_name %></td>
    </tr>
    <tr>
      <td>Phone Number</td>
      <td><%=h @person.phone_number %></td>
    </tr>
    <tr>
      <td>Address</td>
      <td><%=h @person.address %></td>
    </tr>
  </table>

<%= link_to 'Back to List', :action => 'index' %> | <%= link_to 'Edit', :action => 'edit', :id => @person.id %>